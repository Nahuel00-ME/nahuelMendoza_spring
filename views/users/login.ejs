

<%- include("../partials/head.ejs") %>
  

<%-include("../partials/header.ejs") %> 
<main>
    <section>
    <div class="caja1-form">
        <div class="container-formulario"> 
            <h2>Login</h2>
        <form action="/users/loginProceso" method="post">
             <label for="email">Email:</label>
              <input type="email" id="email" name="email" value="<%= locals.email ? email : null %>" required>
              <small style="color:red" id="error-email">
                <p><%= locals.error && error.email ? error.email.msg : null %></p>
            </small>
              
              <label for="password">Contraseña:</label>
              <input type="password" id="contrasena" name="contrasena" value="<%= locals.contrasena ? contrasena :null %>">
              <small  style="color:red" id="error-password">
                <%= locals.error && error.password ? error.password.msg : null %>
                
            </small>
              <button type="submit">Ingresar</button> 
                </form>
        </div>
    </div>
</section>
    </main>

    <%-include("../partials/footer.ejs") %> 
       
    

        
    